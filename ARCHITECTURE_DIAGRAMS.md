# 🌾 Sowing Assessment System - Architecture Diagrams

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     AUTOMATED CROP SOWING ASSESSMENT SYSTEM                  │
│                    Government of Andhra Pradesh Agriculture                  │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────────┐
                              │  External Inputs │
                              └──────┬───────────┘
                                     │
                  ┌──────────────────┼──────────────────┐
                  │                  │                  │
            ┌─────▼─────┐    ┌──────▼──────┐    ┌─────▼─────┐
            │ Satellite  │    │  e-Panta    │    │  Weather  │
            │   Data     │    │  Records    │    │   Data    │
            └─────┬─────┘    └──────┬──────┘    └─────┬─────┘
                  │                  │                  │
                  └──────────────────┼──────────────────┘
                                     │
            ┌────────────────────────▼────────────────────────┐
            │                                                   │
            │    ╔════════════════════════════════════╗        │
            │    ║   SPRING BOOT BACKEND (Java 17)   ║        │
            │    ╚════════════════════════════════════╝        │
            │                                                   │
            │  ┌─────────────────────────────────────────┐    │
            │  │         REST API Controllers            │    │
            │  ├─────────────────────────────────────────┤    │
            │  │ • AuthController      (/api/v1/auth)   │    │
            │  │ • ParcelController    (/api/v1/parcels)│    │
            │  │ • ClassifyController  (/api/v1/classif)│    │
            │  │ • AnalyticsController (/api/v1/analyt) │    │
            │  │ • EPantaController    (/api/v1/epanta) │    │
            │  │ • AlertsController    (/api/v1/alerts) │    │
            │  └─────────────────────────────────────────┘    │
            │            │          │         │                │
            │            ▼          ▼         ▼                │
            │  ┌──────────────────────────────────────┐       │
            │  │      Business Logic Services         │       │
            │  ├──────────────────────────────────────┤       │
            │  │ • AuthService                        │       │
            │  │ • ParcelService                      │       │
            │  │ • CropClassificationService (NDVI)  │       │
            │  │ • AnalyticsService                   │       │
            │  │ • EPantaValidationService            │       │
            │  │ • AlertGenerationService             │       │
            │  └──────────────────────────────────────┘       │
            │            │          │         │                │
            │            ▼          ▼         ▼                │
            │  ┌──────────────────────────────────────┐       │
            │  │   JPA Repositories & Entities        │       │
            │  ├──────────────────────────────────────┤       │
            │  │ • ParcelRepository → Parcel.java    │       │
            │  │ • NDVIRepository → NDVIRecord.java  │       │
            │  │ • EPantaRepository → EPantaRecord   │       │
            │  │ • UserRepository → User.java        │       │
            │  │ • AlertRepository → Alert.java      │       │
            │  │ • + 8 more repositories             │       │
            │  └──────────────────────────────────────┘       │
            │                    │                             │
            └────────────────────┼─────────────────────────────┘
                                 │
            ┌────────────────────▼────────────────────┐
            │   PostgreSQL 16 + PostGIS 3.4          │
            │   (Spatial Database)                    │
            ├────────────────────────────────────────┤
            │  Tables:                                │
            │  • parcels (Polygon geometry)          │
            │  • districts (MultiPolygon geometry)   │
            │  • ndvi_records (vegetation index)     │
            │  • satellite_observations (tiles)      │
            │  • e_panta_records (government data)   │
            │  • users, roles (authentication)       │
            │  • alerts (anomaly detection)          │
            │  • + 6 more tables                     │
            │                                         │
            │  Spatial Indexes (GIST):               │
            │  • idx_parcels_boundary                │
            │  • idx_districts_boundary              │
            │  • idx_satellite_obs_boundary          │
            │  + 2 more                              │
            └────────────────────┬────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
        ┌───────────▼────────────┐  ┌────────▼──────────┐
        │   PgAdmin (Port 5050)  │  │   PgAdmin         │
        │   Database UI          │  │   admin@ap.gov.in │
        └────────────────────────┘  └───────────────────┘
```

## Frontend Architecture

```
┌────────────────────────────────────────────────────────────────┐
│         REACT TYPESCRIPT FRONTEND (Vite Build)                │
│         Port: 3000                                             │
└────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────▼──────────┐
                    │    React Router    │
                    │    (v6 Routes)     │
                    └─────────┬──────────┘
                              │
        ┌─────────────────────┼──────────────────────┐
        │                     │                      │
   ┌────▼────┐         ┌──────▼───────┐      ┌──────▼──────┐
   │ /login  │         │   Protected  │      │   Protected │
   └────┬────┘         │   Routes     │      │   Routes    │
        │              └──────┬───────┘      └──────┬───────┘
        │                     │                      │
   ┌────▼────────────┐   ┌────▼─────────────────────▼─────────┐
   │  Login.tsx      │   │      App Main Routes                │
   │ - Auth form     │   ├─────────────────────────────────────┤
   │ - JWT storage   │   │ / → Dashboard.tsx                   │
   └─────────────────┘   │ /fallow-analysis → FallowAnalysis   │
                         │ /epanta-comparison → EPantaComp     │
                         │ /river-basins → RiverBasinIntel     │
                         │ /spatial-analysis → SpatialAnalysis │
                         │ /settings → Settings                │
                         └────────┬────────────────────────────┘
                                  │
                    ┌─────────────▼────────────────┐
                    │   Dashboard (Main Page)      │
                    ├──────────────────────────────┤
                    │                              │
            ┌───────┼───────┬────────┬────────┐   │
            │       │       │        │        │   │
       ┌────▼──┐ ┌──▼──┐ ┌─▼──┐ ┌──▼─┐ ┌─▼──┐  │
       │Sidebar│ │Map  │ │Ctrl│ │Leg │ │Ana │  │
       └───────┘ └─┬──┘ └────┘ └────┘ └────┘  │
                  │                            │
        ┌─────────▼──────────────────┐        │
        │   MapContainer.tsx         │        │
        │   (Leaflet GIS Map)        │        │
        ├────────────────────────────┤        │
        │  Layers:                   │        │
        │  • ParcelLayer             │        │
        │  • NDVILayer (heatmap)     │        │
        │  • RiverLayer              │        │
        │  • FallowLayer             │        │
        │  • DistrictLayer           │        │
        │                            │        │
        │  Controls:                 │        │
        │  • LayerControl            │        │
        │  • LegendControl           │        │
        │  • DraggableLegend         │        │
        └────────┬───────────────────┘        │
                 │                            │
    ┌────────────▼──────────────────┐        │
    │   Redux Store (Redux Toolkit) │        │
    ├───────────────────────────────┤        │
    │ • mapSlice                    │        │
    │   - center, zoom              │        │
    │   - selectedLayers            │        │
    │   - activeParcel              │        │
    │                               │        │
    │ • dataSlice                   │        │
    │   - parcels[]                 │        │
    │   - ndviRecords[]             │        │
    │   - alerts[]                  │        │
    │   - analytics                 │        │
    └───────────┬───────────────────┘        │
                │                            │
    ┌───────────▼──────────────────────┐    │
    │   Axios API Service              │    │
    │   (src/services/api.ts)          │    │
    ├──────────────────────────────────┤    │
    │ • HTTP interceptor (JWT)         │    │
    │ • Base URL: localhost:8080/api/v1│    │
    │                                  │    │
    │  Endpoints:                      │    │
    │  • POST /auth/login              │    │
    │  • GET /parcels/{id}             │    │
    │  • GET /analytics/district/{id}  │    │
    │  • POST /classification/trigger  │    │
    │  + more...                       │    │
    └──────────┬───────────────────────┘    │
               │                            │
    ┌──────────▼──────────────────────┐    │
    │  Utilities & Styling            │    │
    ├──────────────────────────────────┤    │
    │ • hooks/ (useAppRedux, etc)      │    │
    │ • types/ (TypeScript interfaces) │    │
    │ • data/ (sample GeoJSON)         │    │
    │ • styles/ (Tailwind CSS)         │    │
    │ • globals.css                    │    │
    └─────────────────────────────────┘    │
                                           │
                                      ┌────▼─┐
                                      │Build │
                                      │ ✓    │
                                      └──────┘
```

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────────┐
│                     DATA FLOW: User → API → Database                      │
└──────────────────────────────────────────────────────────────────────────┘

USER INTERACTION FLOW:
═════════════════════

1. USER LOGIN
   ┌─────────────────┐         ┌──────────────┐       ┌────────────────┐
   │ Login.tsx       │────────▶│ AuthService  │──────▶│ UserRepository │
   │ username/pwd    │         │ BCrypt verify│       │ JPA Query      │
   └────────┬────────┘         └──────┬───────┘       └────────┬───────┘
            │                         │                        │
            │                         │    ┌──────────────────┘
            │                         │    │
            │                    ┌────▼────▼──────┐
            │                    │ PostgreSQL DB  │
            │                    │ SELECT * FROM  │
            │                    │ users WHERE    │
            │                    │ username = ?   │
            │                    └────┬───────────┘
            │                         │
            │                    ┌────▼──────────┐
            │                    │ JwtToken Gen  │
            │                    │ Expires: 24h  │
            │                    └────┬──────────┘
            │                         │
            │◄────────JWT Token───────┘
            │
            └─▶ localStorage.setItem('authToken', jwt)


2. VIEW PARCELS & NDVI DATA
   ┌──────────────────┐      ┌──────────────────┐     ┌────────────────┐
   │ Dashboard.tsx    │─────▶│ MapContainer.tsx │────▶│ ParcelLayer.tsx│
   │ mounted          │      │ useEffect        │     │ GeoJSON render │
   └────────┬─────────┘      └────────┬─────────┘     └────────┬───────┘
            │                         │                        │
            │                    ┌────▼──────────────────────┐ │
            │                    │ axios.get(               │ │
            │                    │  /api/v1/parcels?        │ │
            │                    │  village=123)            │ │
            │                    │ Headers: JWT token       │ │
            │                    └────┬───────────────────┬──┘ │
            │                         │                   │    │
            │    ┌────────────────────┘                   │    │
            │    │                                        │    │
            │ ┌──▼──────────────────────────────────┐   │    │
            │ │ Spring Security Filter               │   │    │
            │ │ • Extract JWT from header           │   │    │
            │ │ • Validate token                    │   │    │
            │ │ • Set SecurityContext               │   │    │
            │ └──┬───────────────────────────────────┘   │    │
            │    │                                        │    │
            │ ┌──▼──────────────────────────────────┐   │    │
            │ │ ParcelController                    │   │    │
            │ │ @PreAuthorize("hasAnyRole(...)")   │   │    │
            │ │ GET /api/v1/parcels/village/{id}   │   │    │
            │ └──┬──────────────────────────────────┘   │    │
            │    │                                       │    │
            │    │ ParcelService.getParcelsByVillage()   │    │
            │    │                                       │    │
            │ ┌──▼──────────────────────────────────┐   │    │
            │ │ ParcelRepository                    │   │    │
            │ │ @Query spatial intersection         │   │    │
            │ │ List<Parcel> findByVillage(Long)   │   │    │
            │ └──┬──────────────────────────────────┘   │    │
            │    │                                       │    │
            │ ┌──▼────────────────────────────────────┐ │    │
            │ │ PostgreSQL + PostGIS                 │ │    │
            │ │ SELECT p.* FROM parcels p            │ │    │
            │ │ WHERE p.village_id = ?               │ │    │
            │ │ USING SPATIAL INDEX (GIST)           │ │    │
            │ └──┬────────────────────────────────────┘ │    │
            │    │                                       │    │
            │    └──────────────────▶ Parcel[]          │    │
            │                        GeoJSON features   │    │
            │                        + geometry         │    │
            │                                           │    │
            │                                      ┌────▼───┘
            │                                      │
            │                            Leaflet renders
            │                            polygons on map


3. CLASSIFY PARCELS WITH NDVI
   ┌────────────────────┐     ┌──────────────────────┐
   │ Satellite Ingestion│────▶│ SatelliteObservation │
   │ SENTINEL-2, LANDSAT│     │ Cloud cover, date    │
   └────────┬───────────┘     └──────────┬───────────┘
            │                           │
            │   ┌──────────────────────▼──────────────┐
            │   │ ClassificationService              │
            │   │ @Scheduled batch processor         │
            │   ├───────────────────────────────────┤
            │   │ 1. Find parcels under satellite   │
            │   │    tile (spatial intersection)    │
            │   │                                   │
            │   │ 2. For each parcel:               │
            │   │    a) Get latest NDVI value      │
            │   │    b) Apply rules:                │
            │   │       if NDVI > 0.5               │
            │   │         → CROPPED                │
            │   │       elsif NDVI > 0.3            │
            │   │         → LIKELY_CROPPED         │
            │   │       else                        │
            │   │         → FALLOW                 │
            │   │    c) Save NDVIRecord             │
            │   │    d) Update Parcel.currentStatus │
            │   │                                   │
            │   │ 3. Generate alerts for FALLOW     │
            │   └───┬───────────────────────────────┘
            │       │
            └──────▶│ PostgreSQL DB Updated
                    │ • ndvi_records INSERT
                    │ • parcels UPDATE status
                    │ • alerts INSERT


4. VIEW ANALYTICS & ALERTS
   ┌─────────────────────┐     ┌────────────────────┐
   │ AnalyticsPanel.tsx  │────▶│ AnalyticsController│
   │ useEffect fetch     │     │ GET /api/v1/       │
   └──────────┬──────────┘     │ analytics/district/│
              │                │ {id}               │
              │                └────────┬───────────┘
              │                         │
              │                  ┌──────▼────────────┐
              │                  │ AnalyticsService  │
              │                  │ • Aggregate stats │
              │                  │ • Calculate ratio │
              │                  └────────┬──────────┘
              │                           │
              │    ┌──────────────────────┘
              │    │
              │ ┌──▼──────────────────────────────┐
              │ │ PostgreSQL Aggregation Query    │
              │ │ SELECT                          │
              │ │   COUNT(*) as total_parcels,   │
              │ │   SUM(area_ha) as total_ha,    │
              │ │   COUNT(CASE WHEN             │
              │ │     current_status='CROPPED')  │
              │ │       as cropped_count,        │
              │ │   ...                          │
              │ │ FROM parcels                   │
              │ │ WHERE village_id IN (          │
              │ │   SELECT id FROM villages      │
              │ │   WHERE mandal_id IN (         │
              │ │     SELECT id FROM mandals     │
              │ │     WHERE district_id=?))     │
              │ └──┬───────────────────────────┘
              │    │
              │    └──▶ Report JSON:
              │         {
              │           total_parcels: 5000,
              │           total_ha: 12500,
              │           cropped_count: 3500,
              │           cropped_pct: 70%,
              │           fallow_count: 1500,
              │           fallow_pct: 30%
              │         }
              │
              └──────▶ Recharts renders
                       bar charts & metrics
```

## Module Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│         BACKEND MODULE INTERACTIONS & DATA FLOWS                     │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│   AUTH MODULE      │
├────────────────────┤
│ User login         │
│ JWT generation     │
│ Role verification  │
└─────────┬──────────┘
          │ Issues JWT
          │
    ┌─────▼────────────────────────────────┐
    │  All Other Modules                    │
    │  (Use JWT for @PreAuthorize checks)   │
    └─────┬────────────────────────────────┘
          │
    ┌─────┼────────────────────────────────────────────────────────┐
    │     │                                                        │
    │  ┌──▼──────────┐   ┌──────────────┐   ┌────────────────┐   │
    │  │ PARCEL      │   │ MONITORING   │   │ CLASSIFICATION │   │
    │  │ MODULE      │──▶│ MODULE       │──▶│ MODULE         │   │
    │  ├─────────────┤   ├──────────────┤   ├────────────────┤   │
    │  │ • Districts │   │ • Satellite  │   │ • NDVI rules   │   │
    │  │ • Mandals   │   │ • NDVI       │   │ • Batch proc   │   │
    │  │ • Villages  │   │ • Crop       │   │ • Status update│   │
    │  │ • Parcels   │   │   Season     │   └────────┬────────┘   │
    │  └─────────────┘   └──────────────┘            │            │
    │         │                  │                   │            │
    │         └──────────────────┼───────────────────┘            │
    │                            │                                │
    │                    ┌───────▼──────────┐                     │
    │                    │  ALERTS MODULE   │                     │
    │                    ├──────────────────┤                     │
    │                    │ • Trigger on:    │                     │
    │                    │   - Mismatches   │                     │
    │                    │   - Critical     │                     │
    │                    │     fallow       │                     │
    │                    │   - Anomalies    │                     │
    │                    └──────────────────┘                     │
    │                                                              │
    │  ┌───────────────┐  ┌──────────────┐  ┌─────────────┐      │
    │  │  EPANTA       │  │  ANALYTICS   │  │ SOIL/WEATHER│      │
    │  │  MODULE       │  │  MODULE      │  │ MODULES     │      │
    │  ├───────────────┤  ├──────────────┤  ├─────────────┤      │
    │  │ • Validation  │  │ • District   │  │ • Soil      │      │
    │  │ • Government  │  │   reports    │  │ • Weather   │      │
    │  │   records     │  │ • Mandal     │  │ • Supporting│      │
    │  │ • Verification│  │   reports    │  │   data      │      │
    │  │   status      │  │ • Aggregation│  └─────────────┘      │
    │  └───────────────┘  └──────────────┘                        │
    │         │                  │                                │
    │         └──────────────────┼────────────────────────────────┘
    │                            │
    │                    ┌───────▼──────────┐
    │                    │   PostgreSQL     │
    │                    │   + PostGIS      │
    │                    └──────────────────┘
```

## Database Relationship Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│              Entity Relationship Diagram (ERD)                      │
└────────────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   ROLES      │
                    ├──────────────┤
                    │ id (PK)      │
                    │ name         │
                    │ description  │
                    └──────┬───────┘
                           │ (1:N)
                           │
                    ┌──────▼───────┐
                    │    USERS     │
                    ├──────────────┤
                    │ id (PK)      │
                    │ username     │
                    │ password     │
                    │ email        │
                    │ role_id (FK) │
                    └──────────────┘


        ┌────────────┐
        │ DISTRICTS  │◄──┐
        ├────────────┤   │
        │ id (PK)    │   │
        │ boundary   │   │ (1:N)
        └─────┬──────┘   │
              │          │
              │ (1:N)    │
              │          │
        ┌─────▼──────┐   │
        │  MANDALS   │───┘
        ├────────────┤
        │ id (PK)    │
        │ boundary   │
        └─────┬──────┘
              │
              │ (1:N)
              │
        ┌─────▼──────┐
        │  VILLAGES  │
        ├────────────┤
        │ id (PK)    │
        │ boundary   │
        └─────┬──────┘
              │
              │ (1:N)
              │
        ┌─────▼───────────┬─────────────────────────────────────┐
        │    PARCELS      │                                      │
        ├─────────────────┤                                      │
        │ id (PK)         │                                      │
        │ boundary        │ (Polygon)                            │
        │ survey_number   │                                      │
        │ farmer_name     │                                      │
        │ area_ha         │ (Auto-calculated from polygon)      │
        │ current_status  │ (CROPPED/LIKELY_CROPPED/FALLOW)     │
        └────┬────────┬──┬────────────────────────────────────┘
             │        │  │
          (1:N)    (1:N)│
             │        │  │
        ┌────▼──┐ ┌──▼──▼──────────────┐  ┌──────────────────┐
        │ SOIL  │ │  NDVI_RECORDS      │  │  E_PANTA_RECORDS │
        │ DATA  │ │ (vegetation index) │  │ (govt records)   │
        ├───────┤ ├────────────────────┤  ├──────────────────┤
        │ id(PK)│ │ id (PK)            │  │ id (PK)          │
        │ pH    │ │ mean_ndvi          │  │ farmer_name      │
        │ N,P,K │ │ classification_st  │  │ crop_name        │
        │       │ │ observation_id(FK) │  │ verification_st  │
        └───────┘ │ satellite_obs(FK)  │  │ season_id(FK)    │
                  └────┬──────────────┘  └─────────┬──────────┘
                       │                           │
                       │ (N:M via observation)     │ (N:M via season)
                       │                           │
        ┌──────────────▼──────────────┐   ┌───────▼──────────┐
        │ SATELLITE_OBSERVATIONS      │   │  CROP_SEASONS    │
        ├─────────────────────────────┤   ├──────────────────┤
        │ id (PK)                     │   │ id (PK)          │
        │ sensor_name                 │   │ name             │
        │ cloud_cover                 │   │ year             │
        │ acquisition_date            │   │ start_date       │
        │ tile_boundary (Polygon)     │   │ end_date         │
        │ file_path                   │   │ active           │
        └────┬──────────────────────┬─┘   └──────────────────┘
             │                      │
             │                      │ (Used by)
             │                      │
             │                 ┌────▼─────────┐
             │                 │ ALERTS       │
             │                 ├──────────────┤
             │                 │ id (PK)      │
             │                 │ alert_type   │
             │                 │ message      │
             │                 │ resolved     │
             │                 └──────────────┘


        ┌──────────────────┐
        │ WEATHER_DATA     │
        ├──────────────────┤
        │ id (PK)          │
        │ village_id (FK)  │
        │ temp_min/max     │
        │ precipitation    │
        │ soil_moisture    │
        └──────────────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│             DOCKER CONTAINER DEPLOYMENT ARCHITECTURE                 │
└──────────────────────────────────────────────────────────────────────┘

              ┌─────────────────────────────────────────┐
              │   Docker Compose (docker-compose.yml)   │
              └──────────┬──────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
   ┌────▼────────┐  ┌────▼────────┐  ┌───▼─────────┐
   │ PostgreSQL  │  │ Spring Boot │  │  pgAdmin    │
   │ Service     │  │ Service     │  │  Service    │
   │ (db)        │  │ (app)       │  │ (pgadmin)   │
   └────┬────────┘  └────┬────────┘  └──────┬──────┘
        │                │                  │
   ┌────▼──────────┐ ┌───▼──────────────┐   │
   │ Container:    │ │ Container:       │   │
   │ postgis:16-3.4│ │ FROM Maven build │   │
   │               │ │ + JRE 21-alpine  │   │
   │ Port: 5432    │ │ Port: 8080       │   │
   ├───────────────┤ ├──────────────────┤   │
   │ Volume:       │ │ Env:             │   │
   │ postgres_data │ │ • DB URL         │   │
   │               │ │ • JWT secret     │   │
   │ Health:       │ │ • Log level      │   │
   │ pg_isready    │ │                  │   │
   └───────────────┘ │ Depends on: db   │   │
                     └──────────────────┘   │
                                            │
                                     ┌──────▼──────┐
                                     │ Container:  │
                                     │ pgadmin4:8.4│
                                     │             │
                                     │ Port: 5050  │
                                     └─────────────┘

              ┌────────────────────────────┐
              │   Network: ap_network      │
              │   (bridge network)         │
              │   Enables inter-container  │
              │   communication by name    │
              └────────────────────────────┘

ENVIRONMENT SETUP:
═════════════════

Production (docker-compose.yml):
  Backend   → http://localhost:8080
  Database  → localhost:5432 (internal only)
  PgAdmin   → http://localhost:5050
  Frontend  → npm run build (separate Nginx)

Development (local):
  Backend   → http://localhost:8080 (Java IDE)
  Frontend  → http://localhost:3000 (Vite)
  Database  → localhost:5432 (Docker or local)
```

## Frontend Component Tree

```
┌─────────────────────────────────────────────────────────────────┐
│                REACT COMPONENT HIERARCHY                         │
└─────────────────────────────────────────────────────────────────┘

index.tsx (React Entry)
    │
    └──▶ App.tsx (Root Router)
         │
         ├──▶ Login.tsx (/)
         │
         └──▶ Protected Routes
              │
              ├──▶ Dashboard.tsx (/)
              │    │
              │    ├──▶ Sidebar.tsx
              │    │    ├──▶ ParcelSearch.tsx
              │    │    └──▶ Filter Controls
              │    │
              │    ├──▶ MapContainer.tsx
              │    │    │
              │    │    ├──▶ ParcelLayer.tsx
              │    │    ├──▶ NDVILayer.tsx
              │    │    ├──▶ RiverLayer.tsx
              │    │    ├──▶ FallowLayer.tsx
              │    │    ├──▶ DistrictLayer.tsx
              │    │    │
              │    │    └──▶ Controls
              │    │         ├──▶ LayerControl.tsx
              │    │         ├──▶ LegendControl.tsx
              │    │         └──▶ DraggableLegend.tsx
              │    │
              │    └──▶ AnalyticsPanel.tsx
              │         ├──▶ Recharts (BarChart)
              │         ├──▶ Recharts (LineChart)
              │         └──▶ Metrics Cards
              │
              ├──▶ FallowAnalysis.tsx (/fallow-analysis)
              │    ├──▶ District Selector
              │    ├──▶ Statistics Table
              │    └──▶ Charts
              │
              ├──▶ EPantaComparison.tsx (/epanta-comparison)
              │    ├──▶ Verified Table
              │    ├──▶ Pending Table
              │    └──▶ Rejected Table
              │
              ├──▶ RiverBasinIntelligence.tsx (/river-basins)
              │    ├──▶ Krishna Basin Stats
              │    ├──▶ Godavari Basin Stats
              │    └──▶ Comparison Charts
              │
              ├──▶ SpatialAnalysis.tsx (/spatial-analysis)
              │    ├──▶ Advanced Query Tools
              │    ├──▶ Buffer Analysis
              │    └──▶ Intersection Tools
              │
              └──▶ Settings.tsx (/settings)
                   ├──▶ Preferences
                   ├──▶ Layer Defaults
                   └──▶ User Profile

HOOKS INTEGRATION:
═════════════════
useAppRedux()     → Typed dispatch & selectors
useGeolocation()  → Browser geolocation
useParams()       → Route parameters (React Router)
useState()        → Local component state
useEffect()       → Side effects & API calls
useSelector()     → Redux store subscriptions
useDispatch()     → Redux actions
```

---

**Generated**: May 27, 2026  
**System Version**: 1.0.0  
**Status**: Production Ready ✅
